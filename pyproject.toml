[tool.pytest.ini_options]
# Make the repo root importable so `import app...` works under a bare `pytest`
# invocation (CI runs `pytest tests/ -v`, not `python -m pytest`, which would
# otherwise not add the cwd to sys.path).
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "strict"
