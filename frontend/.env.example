# Base URL for the genai-agents API when NOT using the Vite dev proxy
# (e.g. a built SPA served from a different origin). Leave empty in dev.
VITE_API_BASE=

# Backend target for the dev proxy (defaults to http://localhost:8000).
VITE_API_TARGET=http://localhost:8000
